name: Validate Changelog

on:
  pull_request:
    branches: [master]
    paths:
      - 'CHANGELOG.md'

jobs:
  validate-changelog:
    name: Validate Changelog Format
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Validate Changelog Format
        run: |
          # Check if CHANGELOG.md exists
          if [ ! -f CHANGELOG.md ]; then
            echo "❌ CHANGELOG.md not found"
            exit 1
          fi
          
          # Check for required sections in Unreleased
          if grep -q "^## \[Unreleased\]" CHANGELOG.md; then
            echo "✅ Unreleased section exists"
          else
            echo "❌ Missing [Unreleased] section"
            exit 1
          fi
          
          # Check if there are no empty version headers
          if grep -q "^## \[\]" CHANGELOG.md; then
            echo "❌ Found empty version header"
            exit 1
          fi
          
          # Check for proper date format in versions (YYYY-MM-DD)
          if grep -qE "^## \[[0-9]+\.[0-9]+\.[0-9]+\] - [0-9]{4}-[0-9]{2}-[0-9]{2}" CHANGELOG.md; then
            echo "✅ Version headers have correct format"
          else
            echo "⚠️  Warning: Check version header dates are in YYYY-MM-DD format"
          fi
          
          # Check for description section
          if grep -q "The format is based on \[Keep a Changelog\]" CHANGELOG.md; then
            echo "✅ Changelog header is present"
          else
            echo "❌ Missing Keep a Changelog header"
            exit 1
          fi
          
          echo "✅ All validation checks passed"
